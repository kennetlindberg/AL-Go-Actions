
# Deploy On Premise
Deploy App to on premise environment
## Parameters
### token (default github.token)
The GitHub token running the action
### instance (required)
Specifies the ServerInstance to install the apps to
### tenant (default 'default')
Specifies the Tenant to install the apps to